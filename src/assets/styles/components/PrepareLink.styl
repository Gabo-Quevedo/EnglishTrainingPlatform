@import '../_variables.styl'

.prepare-link
  height 45px
  width 100%
  text-align center
  a
    text-decoration none
    .prepare-link-container 
      display flex
      justify-content center
      align-items center
      margin 0 auto
      height 100%
      width 100%
      max-width 900px
      min-width 320px
      background-color alter-black
      text-decoration none
      color train-yellow
      h1
        font-size 20px
        font-weight: 300
